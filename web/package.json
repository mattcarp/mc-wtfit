{
  "name": "wtfit-web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "copy-site": "node scripts/copy-site.mjs",
    "dev": "npm run copy-site && next dev",
    "build": "npm run copy-site && next build",
    "start": "node scripts/migrate.mjs && next start",
    "migrate": "node scripts/migrate.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node --test --experimental-strip-types tests/"
  },
  "dependencies": {
    "@ai-sdk/anthropic": "^2",
    "@ai-sdk/google": "^2",
    "@ai-sdk/openai": "^2",
    "@clerk/nextjs": "^6",
    "ai": "^5",
    "next": "^15.5",
    "postgres": "^3.4",
    "react": "^19",
    "react-dom": "^19",
    "server-only": "^0.0.1",
    "zod": "^3.25"
  },
  "devDependencies": {
    "@types/node": "^22",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "typescript": "^5"
  }
}
